{
  "name": "paysplit",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix frontend\"",
    "install:all": "npm i --prefix server && npm i --prefix frontend",
    "start:server": "npm run start --prefix server",
    "start:client": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
